{
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++.exe build active file",
            "command": "C:\\msys64\\ucrt64\\bin\\g++.exe",
            "args": [
                "-std=c++11",
                "-fdiagnostics-color=always",
                "-g",
                "${file}",                                                            //Arquivo fonte ativo a ser compilado
                "${workspaceFolder}\\imgui\\*.cpp",                                   //Arquivos principais do ImGui
                "${workspaceFolder}\\imgui\\backends\\imgui_impl_sdl3.cpp",           //Backend do SDL3
                "${workspaceFolder}\\imgui\\backends\\imgui_impl_opengl3.cpp",        //Backend do OpenGL3
                "-I${workspaceFolder}\\imgui",                                        //Diretório principal do ImGui para includes                   
                "-I${workspaceFolder}\\imgui\\backends",                              //Diretório dos backends do ImGui para includes
                "-I${workspaceFolder}\\SDL\\include",                                 //Diretório do SDL3 para includes
                "-L${workspaceFolder}\\build",                                        //Diretório das libs compiladas do SDL3
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe",
                "-lSDL3",                                                             //Linkando a lib do SDL3                 
                "-lopengl32"                                                          //Linkando a lib do OpenGL               
            ],
            "options": {
                "cwd": "${fileDirname}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "Task generated by Debugger."
        }
    ],
    "version": "2.0.0"
}